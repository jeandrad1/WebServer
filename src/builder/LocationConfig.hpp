#ifndef LOCATIONCONFIG_HPP
# define LOCATIONCONIG_HPP

#include <string>
#include <vector>

class LocationConfig
{
    public:
        std::string root;
        std::vector<std::string> root;
};

#endif